interface:
  display_name: "Sub2API Image Generation"
  short_description: "Stream, save, and validate images through Sub2API HTTP"
  default_prompt: "Use $sub2api-imagegen to stream an image through the current Codex Sub2API configuration and save the validated result in the workspace."
