# Fork 版本与发布规则

本仓库是 `Wei-Shaw/sub2api` 的二开版本，版本号必须体现对应的上游正式版本。

## 版本格式

- Fork Tag 使用 `v<上游版本>-kc.<修订号>`，例如 `v0.2.4-kc.1`。
- 同一上游版本下，每次 Fork 发布递增修订号：`v0.2.4-kc.1`、`v0.2.4-kc.2`。
- 上游发布新版本后，Fork 修订号从 1 重新开始：`v0.2.5-kc.1`。
- 禁止创建不符合此格式的 Fork Release Tag，禁止覆盖或强制移动已发布 Tag。

版本顺序定义为：

```text
v0.2.4 < v0.2.4-kc.1 < v0.2.4-kc.2 < v0.2.5 < v0.2.5-kc.1
```

## 上游同步

- 只同步上游最新的正式 GitHub Release Tag，不自动合并上游未发布的 `main`。
- Fork Tag 对应的提交必须包含其声明的上游 Tag。
- 自动合并发生冲突时必须停止，不得自动解决、强推或覆盖 Fork 历史。

## 自动发布

- Fork 发布必须全自动完成，不依赖人工触发；`workflow_dispatch` 仅可用于自动 Workflow 之间的内部调用。
- Fork `main` 出现尚未发布的提交时，自动计算并创建下一个 `kc` Tag。
- 上游发布新正式版本时，自动合并对应 Tag，并创建该上游版本的 `kc.1`。
- 每个 Tag 和 Release 都是不可变的，不得用新构建覆盖旧附件。
- Docker `latest` 指向最新 Fork Release；具体版本 Tag 用于固定版本和回滚。
