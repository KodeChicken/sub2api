# 独立版本与发布规则

本项目独立开发和发布。上游 `Wei-Shaw/sub2api` 仅作为代码更新来源，不参与本项目的版本命名。

## 版本格式

- Release Tag 使用 `v0.0.<修订号>`，从 `v0.0.1` 开始逐次递增。
- 每次发布只增加修订号，例如 `v0.0.1`、`v0.0.2`、`v0.0.3`。
- Tag、GitHub Release 和具体版本镜像均不可覆盖或强制移动。

## 上游同步

- 每 30 分钟检查一次上游最新的正式 GitHub Release。
- 只合并上游正式 Release Tag 对应的代码，不同步上游未发布的 `main`。
- 新的上游 Release 成功合并后，才创建本项目的下一个版本并发布。
- `main` 上的普通开发提交不创建 Tag、不增加版本、不触发发布。
- 自动合并发生冲突时必须停止，不得自动解决、强推或覆盖历史。

## 自动发布

- 首次独立发布为 `v0.0.1`，后续版本从现有独立 Tag 自动计算。
- 发布说明必须记录对应的上游 Release、上游提交和本项目提交。
- Docker `latest` 指向最新正式发布；具体版本 Tag 用于固定版本和回滚。
- 发布失败时重试同一个 Tag，不得通过增加版本号规避失败。
